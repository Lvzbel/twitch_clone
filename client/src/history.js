import { createBrowserHistory } from 'history';

// We create a history object so we can easily use it in our redux actions,
// instead of passing as an argument from our components
export default createBrowserHistory();